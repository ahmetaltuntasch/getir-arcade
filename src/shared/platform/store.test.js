import test from 'node:test';import assert from 'node:assert/strict';import{calculateRunXp,createArcadeStore,levelForXp,normalizeRun}from'./store.js';
const memory=()=>{let value=null;return{getItem:()=>value,setItem:(_,v)=>value=v}};
test('xp is normalized per game and capped',()=>{assert.equal(calculateRunXp({gameId:'getir-rush',score:1000,metrics:{deliveries:10}}),110);assert.equal(calculateRunXp({gameId:'depo-tetris',score:1000,metrics:{rowsCleared:3}}),105);assert.equal(calculateRunXp({gameId:'depo-tetris',score:999999,metrics:{rowsCleared:99}}),400)});
test('duplicate run is idempotent',()=>{const store=createArcadeStore(memory()),run=normalizeRun({clientRunId:'one',gameId:'getir-rush',score:100,durationSeconds:180,metrics:{deliveries:2}});store.recordRun(run);store.recordRun(run);assert.equal(store.getSnapshot().totalRuns,1)});
test('tasks and levels progress from runs',()=>{const store=createArcadeStore(memory());store.recordRun({clientRunId:'two',gameId:'getir-rush',score:100,durationSeconds:180,metrics:{deliveries:10}});const s=store.getSnapshot();assert.equal(s.dailyTasks[0].completed,true);assert.equal(s.dailyTasks[1].completed,true);assert.equal(s.careerXp,351);assert.equal(levelForXp(2000),3)});
test('guest runs enter the outbox once after account creation',()=>{const store=createArcadeStore(memory());store.recordRun({clientRunId:'guest',gameId:'depo-tetris',score:500,durationSeconds:180,metrics:{rowsCleared:2}});store.setAccount({nickname:'oyuncu',userId:'u1'});assert.equal(store.getSnapshot().outbox.length,1);store.markSynced(['guest']);store.setAccount({nickname:'oyuncu',userId:'u1'});assert.equal(store.getSnapshot().outbox.length,0)});
